import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const AddSupportTypeForm = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Add Support Type Form</Typography>
      </CardContent>
    </Card>
  );
};

export default AddSupportTypeForm;